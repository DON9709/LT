//
//  ContentView.swift
//  LT
//
//  Created by 이돈혁 on 5/19/25.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("한/영 전환이 빨라졌어요!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
